using UnityEngine;

public class connection_script : MonoBehaviour
{
    public NodeSystem.Line Line { get; set; }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
